# Real company revenue data — never on GitHub (lives in ws + analytics safepoint + locally)
thoughts/shared/weekly-report-v1/*.html
scripts/weekly_report/sample_data.json
**/snapshot_*.json
.cache/
__pycache__/

# Slack alert posted-message ledger (runtime state)
alert/posted_ledger.json

# Weekly flagged CSV exports (regenerated per run)
flagged_*.csv

# Follow-up draft/posted runtime artifacts (date-stamped, like posted_ledger.json)
alert/followup_drafts_*.json
alert/followup_drafts_*.txt
alert/followup_posted_*.json
