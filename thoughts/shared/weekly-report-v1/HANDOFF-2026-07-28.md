# Weekly Market Report — Handoff (W29, week of 2026-07-20)

Written 2026-07-28. Repo: `~/market-weekly-report-skill`. **Publish only from `main`.**
Deploy dir: `~/analytics/market-notebook-v2`. Ledger (live, SSO-gated): market-notebook.vercel.app/weekly

## 1. W29 shoot — DONE and live
- **13 markets + headout** built, published, deployed, and alerted. Ledger has **14 entries** for 2026-07-20.
- The 10 original pilots + **3 new MENA-subdivision pilots** added this cycle: **GCC, North Africa, Rest of MEA** — all alert to `#mkt-mena-expansion-internal` (C0889D22PM5), separate from UAE's `#mkt-mena` (per Shivang).
- **SEA** was recovered after a build crash (see below) — full report + alert, minus per-CE drawers (transient CE-517 corruption).
- All 89+11 S3 Slack cards render in the 9-market + SEA reports (§2 + CE drawers).

## 2. Commits pushed to `main` this session
- `a42a839` Merge preflight-w29 — 5 fixes: full-waste orders guard, sentiment emoji (🟢/🔴/➖) + green Gains header, Overall-ROI-first + grouped metrics, dup-alert ledger guard, deploy-before-sweep gate
- `92848ae` buckets: full-waste guard uses **CM1** (conversion value), not orders — composites (Paris/Sydney) were false-flagged
- `7fad4c7` weekly_alert: footnote "Top-5 ranked by 4wk-or-WoW" (Parinita's KSC-in-drops flag; kept original dual-lens logic)
- `88c44d1` build_snapshot: **graceful drawer degradation** on byte-cap (the SEA fix — one bad query no longer kills the run)
- `13a25b7` render: auto-load the S3 Slack sidecar at render time (was build-time only → cards dropped on re-render)
- `9b5e79c` publish_weekly: **per-week report archives + Ledger cell links** (past weeks were unreachable)
- `71d8a06` markets: add GCC / North Africa / Rest of MEA pilots

**Not yet pushed:** the token still needs rotation (below). No uncommitted code changes pending.

## 3. IN-PROGRESS — Italy Losing-Money status/comment sync (NOT resolved)
Slack thread (Parag/Aaradhya/Anik, Italy channel): Parag saw the Losing Money bucket showing **all "intentional"** and asked for a 1-line "why". Aaradhya: "yet to show the comment section for this bucket on report." Anik: "**statuses on my report show different from the screenshot**."

**What I found (partial):**
- The GM-notes Sheet (`config.NOTES_SHEET_ID = 1hC_IAsJrlPcpFv5K49eRtcwgK6i_DkAt4ZvETxlK-s8`, via Apps Script) stores **only free-text comments** — columns: `market_slug, ce_id, ce_name, week_start, note, author, updated, slack_*`. **No status/bucket/"intentional" column.** Italy has **0 rows**.
- The report's **ACTION dropdown** (Intentional/Scale down) + bucket actions live in **localStorage**: `template/report_template.html` ~L601 `actionsData[slug][bucket::ceId] = {checkbox, note, status, owner, updated}`; `wrGetAction(ceId)=getAction('losing_money',ceId).status` (~L655). A comment at L601 says "localStorage cache **+ Sheet sync**" — **unverified whether status actually writes to a Sheet or is localStorage-only.**

**Working hypothesis:** ACTION statuses are localStorage-per-browser → Anik sees his selections, Parag's screenshot is a different session/default → no shared source of truth → the mismatch. And the per-CE comment/"why" for the losing_money bucket may not render on the report yet.

**NEXT STEPS to finish this:**
1. Confirm whether `getAction`/bucket status **POSTs to a Sheet** (grep template for the `/exec` fetch in the action-save path) or is localStorage-only. If localStorage-only → that's the root cause; needs Sheet persistence for a shared status.
2. Check whether the report renders the per-CE **comment/why** inside the losing_money bucket (Aaradhya's ask).
3. Trace where Parag's "all intentional" screenshot came from (a Sheet tab? default state?).
4. Deliver: (a) shared/persisted statuses so report == screenshot, (b) the 1-line "why intentional" field surfaced on-report.

## 4. Open / parked follow-ups
- 🔑 **Rotate the Slack alert token** — `xoxb-…R75Q` pasted multiple times, in transcript.
- **SEA data-eng escalation** — CE **517 (USS Singapore)** row-duplication in `mixpanel_user_page_funnel_progression` for 2026-07-14→17 (07-14: 1.38B rows / 4K users; pure dupes, metrics fine, cost-only). Draft message ready; Omni proof: /e/1:eMeIgf34/1, /e/1:QF9iBdM6/1, /e/1:owwV0ZxL/1.
- **Central Live Entertainment** ($434K/wk) — biggest uncovered market; promotable pilot → `#pod-live-entertainment` (C042A57T52Q). LatAm/Africa long-tail have no GM channel → leave headout-only.
- **weekly_rca_helper hardening** — batched query dies if one mover CE is corrupt/oversized (worked around 517 by hand). Same pattern as the drawer degradation.
- **post_message --update list/str bug** — `parent_ts` clobbered to str → AttributeError on 2nd message in an `--update` (harmless here; MSG1 updated fine first).
- **Varun Overall-ROI** thread reply in #team-central-biz (the framing he asked for is now live).
- **#7 bucket-table sorting** → `worktree-diagnostic`.
- **Notes Sheet archival** TODO — now 13 markets, grows faster.

## 5. Operational notes / gotchas
- **Adding a pilot market** = wire 5 places (config.MARKETS, publish_weekly.MARKET_META, weekly_alert.LEDGER_SLUG + MARKET_TEAM, market_channels.json). See memory `adding-a-pilot-market.md`.
- **Bot must be invited first:** alert bot (Revenue/CVR Alerts, `U07E15NCZNK`) can't self-join (token lacks `channels:join` → `missing_scope`; posting fails `not_in_channel`). A human must `/invite` it to any new channel.
- **SEA build** recurs on the byte cap until the 517/07-14 dupes are cleaned — graceful degradation keeps it building (drawers skipped).
- Publish flow: `weekly_market_report.py <slug>` → `publish_weekly.py <slug>` → `vercel deploy --prod --cwd ~/analytics/market-notebook-v2` → alert (`weekly_alert` → `weekly_rca_helper` → `post_message --slug --week --channel`).
