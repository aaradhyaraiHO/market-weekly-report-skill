# HANDOFF 2026-08-10 — Weekly Report: perf-sheet drift-fix rollout + open items

## Current state — main @ `e2172e4` (pushed)
Shipped & live: **C5** losing-money criterion · **4 markets** (benelux/nordics/south_america/
mexico_central_america) + alerts · `export_full_lm` wired into `publish_weekly all` · CE-drawer
**Note history + Action log** · (earlier) repaint fix · campaign-category · 3×/day re-export cron ·
headout alert in the sweep · matrix Sun-Sat + clickable cells.
- **Action log takes effect on the NEXT render** (deployed HTML is static). Re-render + redeploy the
  current-week reports to make it live this week; else it appears on Monday's run.

## ⚠️ ACTIVE PROBLEM — perf-final drift (THE priority)
`export_perf_sheet.write_weekly_tab` (on main) writes data columns **by row position** and re-sorts
every run, leaving perf's columns (43 Perf action / 44 Perf comment / 45 Final action) pinned by row
→ perf finals slide onto the WRONG CE whenever order changes. The **3×/day cron keeps doing this**,
so Adi's finals drift continuously. Proven: Kennedy [3111] moved row 234→145 and inherited a bogus
"Pause & review"; 5 PM vs 7 PM snapshots had **0/17** final↔CID overlap (full scramble).

**Fix is BUILT + TESTED — branch `perf-cid-keying` @ `8ece0a4` (NOT on main):** CID-keyed
read-merge-write in `write_weekly_tab` — reads perf cells by CID, keeps existing CIDs in place
(new appended, no mid-week reshuffle), re-places each perf cell on its CID's row, adds a
"Final action (last wk)" col. Read-only test vs the live tab: **0 drift vs 36 misattributions** under
the old code, order preserved. **Must NOT deploy on the drifted sheet** — its first read would lock in
the current drift. So the rollout restores alignment FIRST.

## THE ROLLOUT (interrupted here to write this handoff — resume from Stage A)
Run from the MAIN checkout (`~/market-weekly-report-skill`, publish-only-from-main). Keep backups.

**A. BACKUP** (do first)
- In-sheet copy: `duplicateSheet` the live tab → `"BACKUP w-c 2026-07-26 (pre-CIDfix 2026-08-10)"`
  (perf sheet `1sXd…`, source gid **1579918324**).
- Local dump: `gws … values get 'w/c 2026-07-26'!A1:AZ400` → `/tmp/perf_sheet_backup_20260810.json`.

**B. FREEZE the cron**
- `launchctl bootout gui/$(id -u)/com.headout.weekly-perf-reexport` (verify: `launchctl list | grep
  weekly-perf-reexport` returns nothing). Reversible.

**C. RESTORE ALIGNMENT** (the precondition — make live perf cols match the last aligned state)
- Source of truth = the **5 PM tab** in Adi's manual-fix sheet `1Qi7nU7U38c8qydxniOD9d9E4HQFjsDWodoZDeDVW5zQ`
  (3 PM == 5 PM agree = aligned; 7 PM fully drifted — discard). In the 5 PM tab, by POSITION:
  Perf action = col 45, Perf comment = col 46, **Final action = col 47 (AT)**. Header row is row 3.
- Build `{cid: (perf_action, perf_comment, final_action)}` from 5 PM.
- Write into the LIVE perf sheet `1sXd…` tab `w/c 2026-07-26`, cols **43/44/45**, matched **by CID**;
  **clear** perf cols for any CID NOT in the 5 PM set. **Verify Kennedy [3111] ends EMPTY**, and the
  17 finals land on the right CIDs (recovered finals also in `/tmp/final_CLEAN_5pm.csv`).

**D. DEPLOY Phase 1**  — `git merge --ff-only perf-cid-keying` on main → `git push`.

**E. VERIFY**  — run `python3 scripts/weekly_report/export_perf_sheet.py --week 2026-07-26 --write`
once (now Phase-1 code); read back and confirm perf cells UNCHANGED and on the right CIDs (the
CID-keyed re-placement should be a no-op on an aligned sheet).

**F. RESUME the cron**  — `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.headout.weekly-perf-reexport.plist`.

## GOTCHA hit during Stage A
`gws` inside a **nested `$(…)` command substitution** failed: *"failed to change group ID: operation
not permitted."* Don't wrap `gws` in nested `$()` in the eval'd shell — run it standalone and parse the
stdout (strip the `Using keyring backend` preamble before `json.loads`), or write the value to a file.
The gid of `w/c 2026-07-26` is **1579918324** (avoids one lookup).

## Phase 2 — store-backed perf layer (durable; the real answer)
Spec: `thoughts/shared/weekly-report-v1/store-backed-perf-layer-spec.md`. Gives perf finals a CE-keyed
store home (like GM), multi-week history, and lights up the drawer Action-log **Perf row**.
- **Needs USER to deploy** an Apps Script `perf_upsert` endpoint + perf fields (external, Google-hosted;
  not in repo). I write the code; then repo side = sheet↔store sync + drawer perf row.
- Decisions locked: enum OPTIONAL (free text fine; report GM dropdown unchanged), 4-week history,
  reflection via CE-drawer Action log.

## Other open items
- **RPC/CM1 fluctuation re-cut** — waiting on `worktree-rpc-cm1-bucket` being finalized/merged; then
  rebuild+republish+refresh alerts for the week.
- **Central Live Entertainment** ($432K/wk, biggest uncovered market) — not wired (channel #pod-live-entertainment C042A57T52Q).
- **Deploy the Action log live this week** (re-render + deploy, ~2 min, no BQ) vs wait for Monday.
- **CE-grouping → monthly** — spec at `~/market-monthly-review-skill/references/all-ce-grouping-port-spec.md`.
- **Slack token rotation** — `xoxb-…` used inline repeatedly this cycle; rotate.
- **Prune stale worktrees**; keep `perf-cid-keying` until the rollout lands.

## Key IDs / files / branches
- main `e2172e4` · branch `perf-cid-keying` `8ece0a4` (drift fix, unmerged).
- Perf Weekly Flagged sheet **1sXd0m2d2Qc5rg99ctpp_hnN5Jg8mtsRAwds_i2ZuwLo** — tab `w/c 2026-07-26`
  (gid 1579918324); ungated tab `LM full (no gate) w-c <week>`.
- Store (GM notes/actions) **1hC_IAsJrlPcpFv5K49eRtcwgK6i_DkAt4ZvETxlK-s8**; Apps Script = `config.NOTES_SCRIPT_URL`.
- Recovery sheet (Adi's snapshots) **1Qi7nU7U38c8qydxniOD9d9E4HQFjsDWodoZDeDVW5zQ** — **5 PM tab = aligned truth**.
- Cron: `com.headout.weekly-perf-reexport` (11/15/19 IST); `scripts/weekly_report/run_perf_reexport.sh` + committed plist.
- Recovered finals: `/tmp/final_CLEAN_5pm.csv` (17) · `/tmp/perf_recon.json`.
- Memories: perf-sheet-drift-fix · perf-sheet-point-in-time · roi-mismatch-week-boundary ·
  custom-grouping-storage · deploy-verify-sso-wall · todo-port-allce-grouping-to-monthly.
